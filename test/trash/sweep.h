#ifndef SWEEP_H // name of file, all caps (this is called a guard - it must be 
                // different for each header file, actual name is irrelevant)
#define SWEEP_H
#include <Servo.h>
#include <OLED.h>

void sweep();

#endif