#ifndef ENCODER_PID_H // name of file, all caps (this is called a guard - it must be 
                // different for each header file, actual name is irrelevant)
#define ENCODER_PID_H
#include <OLED.h>
#include <STM32encoder.h>

void goStraight(int speed);

#endif