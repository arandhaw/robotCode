#ifndef IR_DETECT_H // name of file, all caps (this is called a guard - it must be 
                // different for each header file, actual name is irrelevant)
#define IR_DETECT_H
#include <ReflectSensor.h>
#include <Motor.h>
#include <OLED.h>

int findMaximum();

#endif